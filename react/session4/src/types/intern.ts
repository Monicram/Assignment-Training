export interface Intern {
  id: number
  name: string
  score: number
  isPresent: boolean
  role: string
}

export interface InternFormState {
  name: string
  score: number
  isPresent: boolean
  role: string
}