// Node.js can:
// 1. Read and write files.
// 2. Create web servers.
// 3. Access the operating system using the process object.

console.log("Node version:", process.version);
console.log("Platform:", process.platform);

// Try adding this and run the file — observe the error
// console.log(document.querySelector("h1"))

// Browser JavaScript can:
// 1. Access and change HTML using the document object.